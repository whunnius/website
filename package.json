{
  "name": "vite-project",
  "version": "0.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "3": "^2.1.0",
    "gh-pages": "^4.0.0",
    "three": "^0.137.5"
  }
}
